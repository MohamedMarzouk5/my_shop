import 'package:flutter/material.dart';

class DataAnalysisView extends StatelessWidget {
  const DataAnalysisView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('mohamed'),
      ),
    );
  }
}
